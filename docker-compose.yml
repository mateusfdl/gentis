services:
  gentis-server:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: gentis-server
    ports:
      - "9000:9000"
      - "8080:8080"
    command: ["serve", "-addr", "0.0.0.0:9000", "-metrics-addr", "0.0.0.0:8080", "-metrics", "true"]
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512m
    healthcheck:
      test: ["CMD", "/gentis", "health", "-addr", "http://localhost:8080"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
    networks:
      - gentis-test

  gentis-relay:
    build:
      context: ..
      dockerfile: Dockerfile
    container_name: gentis-relay
    ports:
      - "9001:9001"
      - "8081:8081"
    command: ["relay", "-addr", "0.0.0.0:9001", "-upstream", "gentis-server:9000", "-metrics-addr", "0.0.0.0:8081", "-metrics", "true"]
    depends_on:
      gentis-server:
        condition: service_healthy
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256m
    healthcheck:
      test: ["CMD", "/gentis", "health", "-addr", "http://localhost:8081"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 5s
    networks:
      - gentis-test

  prometheus:
    image: prom/prometheus:latest
    container_name: gentis-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml:ro
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=7d"
      - "--storage.tsdb.min-block-duration=2m"
      - "--query.lookback-delta=30s"
    depends_on:
      - gentis-server
      - gentis-relay
    networks:
      - gentis-test

  grafana:
    image: grafana/grafana:latest
    container_name: gentis-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
      - GF_LIVE_MAX_CONNECTIONS=100
      - GF_LIVE_ALLOWED_ORIGINS=*
      - GF_DASHBOARDS_MIN_REFRESH_INTERVAL=100ms
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
    depends_on:
      - prometheus
    networks:
      - gentis-test

networks:
  gentis-test:
    driver: bridge
